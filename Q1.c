//1. Write a program to check whether a number is even or odd.
#include <stdio.h>

int main() {
    int num = 15;  
    if (num / 2 == 0 )
	 {
        printf("%d is Even\n", num);
      }
	 else
	   {
	   
        printf("%d is Odd\n", num);
       }

    
}
